<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.minyiyang.finalproject">

    <uses-permission android:name="android.permission.INTERNET" />

    <supports-screens android:requiresSmallestWidthDp="600" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity
            android:name=".MainActivity"
            android:launchMode="standard" />
        <activity
            android:name=".ListQuestions"
            android:launchMode="standard" />
        <activity android:name=".EditeMultiQuest" />
        <activity android:name=".EditeNumQuest" />
        <activity android:name=".EditeTFQuest" />
        <activity android:name=".ShowQuest" />
        <activity android:name=".DialogStatistics" />
        <activity android:name=".AsyncTask" />
        <activity
            android:name=".TestToolbar"
            android:label="@string/title_activity_test_toolbar"
            android:theme="@style/AppTheme.NoActionBar">
        </activity>
        <activity android:name=".StartActivity"
            android:label="@string/app_name"
            android:theme="@style/AppTheme.NoActionBar">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>